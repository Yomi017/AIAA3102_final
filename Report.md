**一、引言与任务背景**

- 课程背景与项目目标：实现一个基于 Qwen3/Qwen3-VL 的多模态 ReAct Agent，用于工具调用、RAG、ALFWorld 环境交互与安全评估。  
- 总体问题设定：  
  - 多模态输入（文本 + 图片）下的复杂推理与工具使用。  
  - 开放环境（Web 工具、RAG、外部 API）带来的不确定性与安全风险。  
- 本项目整体贡献概述：  
  - 轻量级、多模态 ReAct Agent 实现。  
  - 可扩展工具体系和 Wiki RAG 管线。  
  - 覆盖 ReAct/RAG/web/安全四类 Benchmark。  

**二、系统整体设计**

- 总体架构图（建议画）：CLI / Web 前端 / FastAPI 后端 / Agent / 工具 / RAG / PromptGuard。  
- 关键模块：  
  - agent.py：ReAct loop、工具调用协议（Action/Action Input JSON）、多轮工具调用。  
  - llm.py：Qwen3 与 Qwen3-VL 封装，多模态输入处理。  
  - tool.py + calculator.py：Google 搜索、时间天气、数学、RAG 等工具统一路由。  
  - rag：文档处理、向量索引构建、检索与上下文拼接。  
  - prompt_guard.py：Prompt 攻击检测与会话锁定。  
  - main.py + frontend：多会话 Web UI 与 WebSocket 交互。  
- 设计目标与约束：  
  - 尽量保持框架轻量（文件级模块，而不是复杂框架）。  
  - 工具、RAG、安全模块可独立替换或扩展。  

**三、关键设计选择说明**

1. **ReAct Prompt 与 Agent 设计**  
   - 固定 Thought → Action → Observation → Final Answer 模式的原因：  
     - 便于解析与调试。  
     - 与 ALFWorld 等环境交互形式自然对齐。  
   - 工具调用格式（JSON 参数、工具名约定）的设计权衡：  
     - 易于使用 Python `json` 解析；  
     - 对大模型格式鲁棒性的考虑（例如允许一定冗余文本，通过正则提取 JSON）。  

2. **多模态模型的选择：Qwen3-VL**  
   - 相比纯文本模型，支持图片输入（任务截图、网页截图等）的优势。  
   - 在实现上的变化：  
     - CLI 的图片命令（`@image:`、`@paste`、`@clear`、`@show`）。  
     - 后端/前端的图片上传与 URL 管理。  
   - 模型加载方式（本地权重 vs 远程）与资源权衡。  

3. **工具系统设计**  
   - 将所有工具集中到 tool.py 的原因：  
     - 统一路由，便于增加/修改工具。  
     - 日志与错误处理集中管理。  
   - 工具类型划分：  
     - 外部 API（Google 搜索、时间/天气）。  
     - 纯计算类（标量/三角/矩阵/定积分等）。  
     - RAG 工具（调用内部向量库）。  
   - 设计考虑：  
     - 工具描述（用于 LLM 选择）。  
     - 工具调用失败时的错误文案与重试策略（目前的实现现状与可改进点）。  

4. **RAG 管线设计**  
   - 文档来源与特点：wiki_docs 中的多篇 AI/ML 相关条目。  
   - 预处理设计：`document_processor.py` 的滑动窗口分块、分块大小与步长的选择理由。  
   - 向量库设计：`sentence-transformers` + `faiss`，选择这套方案的考虑（效果 vs 部署开销）。  
   - 检索与拼接：`rag_engine.py` 如何选择 top-k、如何拼接到 LLM 提示中。  

5. **安全模块 PromptGuard 设计**  
   - 四类威胁类型：Prompt Injection / Insecure Output Handling / Insecure Plugin or Tool Usage / Excessive Agency。  
   - 规则/模型设计思路（基于模式匹配 + Heuristic 的可能实现）。  
   - 与 Agent 的集成：  
     - 在输入侧拦截用户指令。  
     - 触发高危时锁定当前会话的策略（config.yaml 中配置）。  

6. **接口与前端设计**  
   - FastAPI 设计：多会话结构、路由划分（聊天、工具列表、文件上传等）。  
   - 前端：基于 Vite + TS + Tailwind 的 Chat UI，设计考量：  
     - 多轮对话展示。  
     - 图片消息显示。  
     - 会话列表与状态展示。  

**四、实验设置与评估方案**

1. **ReAct / 环境交互评估（ALFWorld）**  
  - 评测脚本：ALFworld.py + ALFworld 数据。  
  - 任务类型示例：放置物体、加热食物、清理物件等。  
  - 评估指标：  
    - 任务成功率（按游戏成功 / 总游戏数）。  
    - 步数统计（总步数、成功步数、动作成功率）。  
  - 三个 session 的实验设置（同一次实验连续运行，结果记录在 `benchmark_results/session_时间戳/`）。  

2. **RAG 评估**  
  - 数据来源与构造方式（针对 wiki_docs 的问答对）。  
  - 可能的自动评估指标：BLEU/ROUGE/简单准确率。  
  - 实际项目中已实现的部分与人工评估补充。  

3. **搜索增强评估（Web Search + RAG）**  
  - 工具与模块：`tool.py` 中的 `GoogleSearchTool` / `TavilySearchTool`，结合 `rag_engine.py`。  
  - 典型任务：事实性问答、时效性信息查询（新闻 / 事件）、需要跨多页面整合的信息检索。  
  - 评估设置：  
    - 仅使用 LLM 内部知识（不调用搜索 / RAG）；  
    - 启用 Web 搜索但不拼接本地向量库；  
    - 同时启用 Web 搜索 + RAG（先外部检索再内部检索）。  
  - 指标与观察：回答准确率、信息时效性、引用内容与实际网页的一致性，错误来源分析（检索失败 vs 推理错误）。  

4. **安全/提示注入评估**  
  - test_prompt_guard.py 和 test_agent_prompt_guard.py 的测试设计。  
  - 攻击样例类别：试图覆盖系统指令、诱导泄露机密、指使关闭安全防护等。  
  - 评估指标：检测率、误报率（可定性描述）。  

**五、实验结果与失败分析**

1. **ALFWorld 结果汇总（多 session 合并）**  
   - 实验数据来源：  
     - `session_20251202_200635` / `20251202_202956` / `20251202_213009`。  
     - 合并方式：按 `game_num` 以**最后一次运行覆盖前一次**，合并到 `benchmark_results/session/`。  
   - 核心结果：  
     - 合并后总游戏数、成功/失败数、成功率。  
     - 总执行步数、成功步数、动作成功率，平均每局步数/成功步数。  
   - 结果解读：  
     - 哪些任务类型成功率高（例如简单“放置类” vs 多步骤“清洗+加热+丢弃”）。  
     - 哪些任务容易失败（例如长链条操作、需要精确定位的任务）。  

2. **典型失败案例分析**  
   - 从 detailed_results.csv 中挑选一到两个失败 game：  
     - 例如步数不多但未完成任务的 case：分析原因可能是：  
       - LLM 误解目标物品；  
       - 工具调用顺序不合理；  
       - 对环境反馈（Observation）理解错误。  
   - 从日志中（如 `logs/session_时间戳/`）挑出一条 ReAct trace，逐步分析每个 Thought / Action 的错误。  

3. **RAG / 安全模块的现象观察**  
   - RAG：  
     - 在有/无向量库的情况下，回答质量的直观对比（可用几个问题示例）。  
     - 检索到的片段是否被正确引用，是否出现“检索到了但没用上”的情况。  
   - 安全：  
     - PromptGuard 成功拦截的一两个示例。  
     - 潜在失败场景（例如非常隐晦的攻击指令）。  

**六、消融实验设计与分析**

1. **有/无检索（RAG Ablation）**  
   - 设置：  
     - 有检索：启用 wiki_vector_db，RAG 工具可用。  
     - 无检索：关闭向量库或禁用 RAG 工具（只靠模型内知识）。  
   - 对比维度：  
     - 知识密集问题的准确率。  
     - 回答中的引用质量（是否能结合具体 Wiki 内容）。  
   - 分析：  
     - RAG 对于长尾知识、课程相关专业知识的加成程度。  
     - 失败案例：即便有 RAG，LLM 仍然忽略检索结果。  

2. **有/无搜索增强（Web Search Ablation）**  
   - 设置：  
     - 关闭 Web 搜索，仅使用本地 RAG 或模型内部知识；  
     - 启用 Google/Tavily 搜索，但不使用本地向量库；  
     - 同时开启 Web 搜索 + RAG（搜索增强检索）。  
   - 对比内容：  
     - 对时效性问题、跨网页聚合问题的回答质量差异。  
     - Web 检索引入噪声信息的概率与其对最终答案的影响。  
   - 讨论：  
     - 什么时候“只用 RAG”已经足够，什么时候必须引入 Web 搜索。  
     - 如何在 Prompt 中显式区分“需要检索的事实”与“需要推理的部分”。  

3. **有/无安全模块（Verifier / Guard Ablation）**  
   - 设置：  
     - 开启 `enable_prompt_guard`：所有输入经过检测，高危时锁定会话。  
     - 关闭 PromptGuard：直接将用户指令传给 LLM。  
   - 对比内容：  
     - 能否成功阻断明显的提示注入攻击。  
     - 误报情况：正常指令被错误拦截的例子。  
   - 讨论：  
     - 安全模块的收益与成本（用户体验、自由度 vs 安全性）。  
     - 未来可以引入更强 Verifier（例如基于第二个模型或规则+LLM 判别器）。  

4. **工具使用策略消融（可选）**  
   - 比较“允许多轮工具调用” vs “限制一次工具调用”的表现：  
     - 对复杂任务的影响（例如需要先检索再计算）。  
   - 可结合已有日志进行定性分析。  

**七、讨论与未来工作**

- 当前系统的主要优势：  
  - 架构清晰、模块化（工具、RAG、安全易替换）。  
  - 多模态支持与 ReAct 框架结合自然。  
  - 已有完整的测试与 Benchmark 管线。  
- 主要局限：  
  - 对工具失败与超时缺乏完备的容错（README 中 TODO 项之一）。  
  - PromptGuard 以规则/启发为主，对复杂攻击仍不够鲁棒。  
  - RAG 目前集中在英文 Wiki 文档，领域扩展有限。  
- 未来改进方向（可对应 README 的 TODO 列表展开）：  
  - 工具调用的超时、重试与熔断机制（提升可靠性）。  
  - 构建更系统化的评估集与自动评分脚本（覆盖更多任务类型）。  
  - 引入多轨迹投票、自洽性检查、多 Agent 协作机制。  
  - 更精细的幻觉控制策略：基于检索置信度和引用粒度来给出答案置信度。  

**八、结论**

- 总结本项目在多模态 ReAct Agent、RAG 检索和安全防护方面的整体设计与实验结果。  
- 强调关键发现：  
  - 工具与检索显著提升了复杂任务的成功率。  
  - 安全模块虽有误报，但对防止明显提示注入有实质效果。  
  - 在 ALFWorld 等交互式环境中，ReAct 策略能够完成一定比例的复杂任务，但仍有规划和理解上的瓶颈。  
- 简要展望：如何将本系统推广到更大规模、更多模态与更真实的应用场景。